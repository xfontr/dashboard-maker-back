const ENDPOINTS = {
  users: {
    router: "/users",
    root: "/",
    logIn: "/log-in",
    refresh: "/refresh",
  },
  tokens: {
    router: "/tokens",
    root: "/",
    verify: "/verify",
  },
};

export default ENDPOINTS;
