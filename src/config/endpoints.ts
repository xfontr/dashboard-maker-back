const ENDPOINTS = {
  users: {
    router: "/users",
    root: "/",
    logIn: "/log-in",
  },
  tokens: {
    router: "/tokens",
    root: "/",
    verify: "/verify",
  },
};

export default ENDPOINTS;
