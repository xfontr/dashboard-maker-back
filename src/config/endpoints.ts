const ENDPOINTS = {
  users: {
    router: "/users",
    root: "/",
    logIn: "/log-in",
    logOut: "/log-out",
    refresh: "/refresh",
  },
  tokens: {
    router: "/tokens",
    root: "/",
    verify: "/verify",
  },
};

export default ENDPOINTS;
